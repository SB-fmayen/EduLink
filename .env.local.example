# Este es un archivo de ejemplo. Copia este archivo a .env.local y reemplaza los valores.
# NO subas el archivo .env.local a tu repositorio de Git.

# Para que el SDK de Admin de Firebase funcione localmente, necesita credenciales.
# 1. Ve a tu proyecto de Firebase > Configuración del proyecto > Cuentas de servicio.
# 2. Haz clic en "Generar nueva clave privada" y guarda el archivo JSON en un lugar seguro de tu proyecto (p. ej., en la raíz, pero fuera de /src).
# 3. Reemplaza 'ruta/a/tu/archivo-de-clave-privada.json' con la ruta real a tu archivo.

GOOGLE_APPLICATION_CREDENTIALS="ruta/a/tu/archivo-de-clave-privada.json"
